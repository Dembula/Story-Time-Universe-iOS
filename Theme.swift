import SwiftUI

// Centralized theme to match the web platform.
// Update these colors to your exact brand palette.
struct Theme {
    static let background = Color.black
    static let foreground = Color.white
    static let accent = Color(red: 0.89, green: 0.35, blue: 0.13) // Brand accent
    static let muted = Color.gray
}
